-- | Supporting functions for turn system.
--
-- Extremely simple at the moment, turns are just integers and there are no
-- actions that take less than one turn.

module RWPAS.Turn
  ( Turns )
  where

-- | Type describing the number of turns.
type Turns = Integer

